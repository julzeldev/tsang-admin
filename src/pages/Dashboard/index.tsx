import DarkModeToggle from '@/components/DarkModeToggle';

const Dashboard = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <DarkModeToggle />
    </div>
  );
};

export default Dashboard;
